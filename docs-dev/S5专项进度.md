# S-5 专项进度 — Parser 产出语言节点（原子切换）

> 专项对应 [`前端拆分与IR分层.md`](前端拆分与IR分层.md) §5 Phase 1b（S-5）。
> 原则：每子步独立回归（全量 41805 绿），逐子步文档 + 提交。
> 基线：`a83263f`（slnx 收口）· 全量 41805 绿（1m41s）。

## 决策（2026-09-02 定稿）

1. **SemanticModel**：按语言拆分到语言库（`CocoaSemanticModel`/`CSharpSemanticModel`，对齐 Roslyn `CSharpSemanticModel`）；`Compilation.GetSemanticModel` 经 `Language.CreateSemanticModel` 分派。
2. **GreenNode.CreateTypedRed**：按语言各一份（Cocoa/CSharp 各自类型化红节点构建器）；Core 仅留 `RawKind:int` 中性基础设施；`Language.CreateTypedRed` 工厂分派。
3. **Monomorphizer 泛型用法扫描**：`Language.CollectGenericUsages(BoundGlobalScope)` 钩子返回中性结构；`IBinder.BindGenericTypeNameForExpansion` 签名改语言中性（`ImmutableArray<SyntaxNode>`）。

---

## P1-1：FunctionSymbol/NamedTypeSymbol Declaration → SyntaxNode?

**目标**：符号 `Declaration` 属性与构造参数由语言具体类型改语言中性 `SyntaxNode?`（共享具体节点是 `SyntaxNode` 子类，天然兼容），为语言节点替换铺路。

**改动**：
- `Cocoa.Core/Symbols/FunctionSymbol.cs`：ctor 参数 + `Declaration` 属性 `FunctionDeclarationSyntax?` → `SyntaxNode?`
- `Cocoa.Core/Symbols/NamedTypeSymbol.cs`：ctor 参数 + `Declaration` 属性 `ClassDeclarationSyntax?` → `SyntaxNode?`
- 消费者适配（仍持共享具体节点，作共享类型转换保持行为不变）：
  - `Cocoa.Core/Compilation/Compilation.cs`（3 处 `Declaration?.Identifier.Location` → `((ClassDeclarationSyntax?)…)?`）
  - `Cocoa.Core/Emit/Native/NativeImportValidator.cs`（2 处 + 补 `using Cocoa.CodeAnalysis.Syntax`）
  - `Cocoa.Core.Cocoa/Binder/Impl/CocoaBinder.cs` / `Cocoa.Core.CSharp/Binder/Impl/CSharpBinder.cs`（`Declaration.Body`/`.Identifier`/`.Identifier.Location` 共 8 处）
  - `Cocoa.Core.Cocoa/Binder/Impl/CocoaBinder.Declarations.cs` / `Cocoa.Core.CSharp/Binder/Impl/CSharpBinder.Declarations.cs`（`Members`/`Identifier.Location` 共 8 处）

**回归**：41805 绿（1m41s）。

**提交**：`639e3b0`（P1-1）

---

## P1-2：IBinder.BindGenericTypeNameForExpansion → ImmutableArray<SyntaxNode>

**目标**：单态化种子绑定的泛型类型名接口语言中性化（`TypeClauseSyntax` 具体类型不出接口）。

**改动**：
- `Cocoa.Core/Binder/IBinder.cs`：`BindGenericTypeNameForExpansion(SyntaxToken, ImmutableArray<TypeClauseSyntax>)` → `ImmutableArray<SyntaxNode>`
- `Cocoa.Core/Binder/Monomorphizer.cs`：调用点 `argumentClauses.Cast<SyntaxNode>().ToImmutableArray()`（+ 补 `using System.Linq`）
- `Cocoa.Core.Cocoa/Binder/Impl/CocoaBinder.Statements.cs` / `Cocoa.Core.CSharp/Binder/Impl/CSharpBinder.Statements.cs`：实现签名同步改中性，内部 `Cast<TypeClauseSyntax>().ToImmutableArray()` 回落共享类型（P1 保持行为不变）

**回归**：41805 绿（1m45s）。

**提交**：`21fdb9a`（P1-2）

---

## P1-3：Language 新增六钩子（语言中性接缝）

**目标**：为 P1-4/P1-5/P2-4/P2-5/P2-6 的消费者语言中性化铺设抽象接缝；语言库先行以共享类型实现保持绿。

**改动**：
- `Cocoa.Core/Language.cs`：新增 6 个 `internal abstract` 钩子——
  - `CreateTypedRed(GreenNode, SyntaxTree, int)`（P2-4 用）
  - `CollectGenericUsages(BoundGlobalScope)`（返回中性 `(SyntaxToken, ImmutableArray<SyntaxNode>)` 对，P2-5 用）
  - `GetDeclaredNamespaceNames(SyntaxTree)`（P2-6 用）
  - `GetRootMembers(SyntaxTree)`（P2-6 用）
  - `CreateSemanticModel(Compilation, SyntaxTree)`（P1-5 用）
  - `GetUnreachableCodeLocation(SyntaxNode)`（P1-4 用）
- 共享 Core 服务：
  - `Cocoa.Core/Diagnostic/UnreachableCodeLocator.cs`：自 `DiagnosticBag.ReportUnreachableCode` 抽取的位置解析（返回 `TextLocation?`）
  - `Cocoa.Core/Syntax/SyntaxTreeServices.cs`：根成员/命名空间名中性收集
  - `Monomorphizer.CollectGenericUsages` 改 `internal static` + 中性返回（`ImmutableArray<SyntaxNode>`），调用点去 Cast
- `Cocoa.Core.Cocoa/CocoaLanguage.cs` / `Cocoa.Core.CSharp/CSharpLanguage.cs`：实现 6 钩子，P1 委托共享服务（`green.CreateTypedRed`/`Monomorphizer.CollectGenericUsages`/`SyntaxTreeServices.*`/`UnreachableCodeLocator`/`new SemanticModel`）

**回归**：41805 绿（1m42s）。

**提交**：`fb39ed2`（P1-3）

---

## P1-4：DiagnosticBag.ReportUnreachableCode → Language.GetUnreachableCodeLocation

**目标**：共享 Core 诊断不再 pattern-match 共享具体节点类型（14 处），改经语言钩子分派。

**改动**：
- `Cocoa.Core/Diagnostic/DiagnosticBag.cs`：`ReportUnreachableCode(SyntaxNode)` 改委托 `node.SyntaxTree.Language.GetUnreachableCodeLocation(node)`，命中即报（原 switch 逻辑已抽取为 `UnreachableCodeLocator`）
- 双语言库钩子已就位（P1-3），P1 委托 `UnreachableCodeLocator` 保持行为不变

**回归**：41805 绿。

**提交**：`7b98a27`（P1-4）

---

## P1-5：SemanticModel 拆分（共享抽象基类 + 语言库子类）

**目标**：按语言拆分语义模型（对齐 Roslyn `CSharpSemanticModel`）；共享 Core 不再 pattern-match 共享具体节点类型（原 17 处）。

**改动**：
- `Cocoa.Core/SemanticModel.cs`：改 `public abstract class`——保留共享基础设施（`BoundBySyntax`/`BoundNodeCollector`/`GetOperation`/`GetDiagnostics`/`ResolveName`/`EnumerateNamespaces`/`ResolveBuiltin`/`GlobalNamespace` 为 protected），`GetTypeInfo`/`GetDeclaredSymbol`/`GetSymbolInfo` 改 abstract
- `Cocoa.Core.Cocoa/Compilation/CocoaSemanticModel.cs` / `Cocoa.Core.CSharp/Compilation/CSharpSemanticModel.cs`：语言专属实现（P1 仍以共享节点类型，行为不变；P2-5 切语言节点）
- `Cocoa.Core/Compilation/Compilation.cs`：`GetSemanticModel` 经 `Language.CreateSemanticModel` 分派
- 双语言库 `CreateSemanticModel` 钩子返回各自子类

**回归**：41805 绿（1m33s）。

**提交**：`0921f99`（P1-5）

---

## P2-1+P2-2：Parser 语言化 + SyntaxTree/IParser 中性化

**目标**：S-5 原子切换第一段——Parser 产出语言节点；`SyntaxTree.Root`/`IParser.ParseCompilationUnit`/`ParseHandler` 语言中性化（抽象 `SyntaxNode`）。

**改动**：
- `Cocoa.Core.Cocoa/Syntax/CocoaParser.cs` / `Cocoa.Core.CSharp/Syntax/CSharpParser.cs`：namespace → 语言命名空间（`Cocoa.CodeAnalysis.Cocoa.Syntax` / `.CSharp.Syntax`），加 `using Cocoa.CodeAnalysis.Syntax;` 共享基础设施；`ParseCompilationUnit()` 返回 `SyntaxNode`
- `Cocoa.Core/Syntax/SyntaxTree.cs`：`ParseHandler` out `SyntaxNode`、`Root` → `SyntaxNode`、`CreateParentsDictionary` 参数改 `SyntaxNode`
- `Cocoa.Core/Syntax/IParser.cs`：`ParseCompilationUnit()` → `SyntaxNode`
- 双 Binder：`st.Root.Members`（3 处）→ `st.Language.GetRootMembers(st)`
- `Compilation.CollectNamespaceNames` → `tree.Language.GetDeclaredNamespaceNames(tree)`；`CocoaRepl` → `Language.GetRootMembers(tree)`；`SyntaxTreeServices` 改共享转换

## P2-4：Green 工厂切语言节点

**改动**：
- `Cocoa.Core.Cocoa/Syntax/Green/CocoaGreenNodeFactory.cs` / `Cocoa.Core.CSharp/Syntax/Green/CSharpGreenNodeFactory.cs`：自 `GreenNode` 抽取的 ~1300 行类型化红节点构建器随迁语言库（各一份，`new XxxSyntax` 解析到语言节点类）
- `Cocoa.Core/Syntax/Green/GreenNode.cs`：`CreateTypedRed` 改委托 `syntaxTree.Language.CreateTypedRed(this, …)`（Core 仅留 RawKind:int 中性基础设施）
- 双 `Language.CreateTypedRed` 钩子 → 各语言工厂

## P2-3+P2-5+P2-6+P2-7+P3（原子翻转主体，一次性落地）

> S-5 原子切换：Parser 产出语言节点后，Root/Binder/钩子/Green 工厂/消费者/共享节点删除/测试迁移必须同批落地（任一环失败破坏基线）。本段为一次工作树变更，最终一次提交。

**P2-3 Binder 副本切语言节点**：
- 双 Binder（各 5 partial）：`using Cocoa.CodeAnalysis.Syntax` → `using Cocoa.CodeAnalysis.Cocoa.Syntax`（/`.CSharp.Syntax`）+ `using SSyntax = Cocoa.CodeAnalysis.Syntax`（共享基础设施别名）；节点 kind 判断 `SyntaxKind.X` → `CocoaSyntaxKind.X`/`CSharpSyntaxKind.X`（各 37 case + 23 非 case）；token 判断保留共享 `SyntaxKind`（`SSyntax.SyntaxKind.X`）

**P2-5 钩子实现切语言节点**：
- 双语言库 `CreateTypedRed` → 各自 Green 工厂（P2-4）
- `CollectGenericUsages`/`GetRootMembers`/`GetDeclaredNamespaceNames`/`GetUnreachableCodeLocation`/`GetDeclarationNameLocation`/`HasDeclaredFacadeModifier` → 语言库按语言节点实现
- 共享 `SyntaxTreeServices`/`UnreachableCodeLocator` 删除（逻辑随迁语言库）

**P2-6 消费者适配**：
- `Compilation.CollectNamespaceNames`/`DeclaredFacade`/3 处 `ClassDeclarationSyntax.Identifier.Location` → 语言钩子；`NativeImportValidator` → 钩子；`CocoaRepl` → `GetRootMembers`（P2-2 已改）；`DiagnosticBag.ReportUnreachableCode` → 钩子（P1-4）

**P2-7 删除共享具体节点类**：
- `Cocoa.Core/Syntax/` 下 75 个共享节点类删除；保留基础设施（SyntaxNode/SyntaxToken/SyntaxTree/SyntaxTrivia/SyntaxKind/SyntaxFacts/SyntaxKindLanguageOwnership/SyntaxWalker/SeparatedSyntaxList/IParser/ILexer/InterpolatedStringPart/Green/*）
- `SyntaxNode.Kind` 改经抽象 `RawKind:int` 具名（语言根类 `override int RawKind` → 语言枚举 int 值），共享消费者读 `Kind` 得对齐值；`SyntaxNode.ToGreen`/`ParseTokens` 根构建改经 `RawKind`/语言工厂

**P3 测试迁移（388 处引用）**：
- `ParserTests`/`ClassBindingTests`/`GenericSyntaxTests`/`ParameterSyntaxTests`/`LambdaSyntaxTests`/`CSharpDialectTests` → 按方言切语言命名空间/别名；`.Kind` 节点比较改语言枚举；`Root` 改 `(CompilationUnitSyntax)` 转换；`AssertingEnumerator`/`CocoaKind()/CSharpKind()` 改 `RawKind`
- `LambdaExpressionSyntax.Body` 改语言根类型（`CocoaSyntaxNode`/`CSharpSyntaxNode`，`Kind` 返语言枚举）
- 双前端契约测试（CO 词在 C# 作标识符等）留 Phase 3 后续补充

**回归**：41805 绿（1m34s）。

**提交**：`<填写 commit>`（P2+P3 原子翻转）
