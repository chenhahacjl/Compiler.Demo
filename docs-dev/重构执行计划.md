# 重构执行计划

> 状态标记：⬜ 未开始 / 🔄 进行中 / ✅ 完成 / ⏭️ 跳过
> 规则：每个步骤完成后**更新本文档进度表并单独提交**（commit message 前缀 `refactor(plan)`）。
> 硬性约束：
> 1. 全程不改外部契约：`cocoa` CLI 参数、`.coa` 格式、`.cocproj`/`.cosln` 格式、方言 API 面（带前缀类型名保持）
> 2. 每阶段独立 PR（本仓库为单人主干流，落地为独立 commit），可回滚；改名（阶段 2）与去重（阶段 3）绝不混合
> 3. 每阶段验收：`build.cmd` 全绿 + 黄金基线一致 + 警告数不增（P0 警告棘轮自动守护）
> 4. 阶段 1 的 bug 修复必须先于阶段 3 的去重，否则两份副本要修两次

---

## 已确认的设计决策

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 前端 Cocoa/CSharp 重复 | 走"共享骨架 + Source Generator 生成语法节点"路线（兼容多语言扩展意图），Parser 不合并（已实质分化） |
| 2 | 解释器（Evaluator） | 提升为独立解释器后端项目，与 IL/Native 同构走 `Register()` 模式 |
| 3 | 项目/命名空间改名 | 全面改名，物理程序集名对齐逻辑命名空间 |
| 4 | 解决方案文件 | 只留 `.slnx`，删除 `.sln` |
| 5 | 单文件文件夹 | 按功能归属归位（Compilation 吸收散文件、IO 并入 Text、MetadataReader 改名 Emit\IL） |

## 目标项目结构

```
src\
├── Cocoa.CodeAnalysis\            ← 原 Cocoa.Core
│   ├── Compilation\    Compilation + SemanticModel + Language + MetadataReference（吸收根目录 3 散文件）
│   ├── Emit\          IlTarget.cs + Emit\IL\（原 MetadataReader\；阶段 4 后写侧下沉）
│   ├── Text\          原 Text 4 文件 + IO\TextWriterExtensions（5 文件）
│   ├── Bound\         原 Binder\（73 文件，Bound 节点 + 分析器）
│   ├── Syntax\        12 文件 + Green\ 6 文件
│   ├── Symbols\       30 文件
│   ├── Diagnostics\   3 文件
│   ├── Lowering\      4 文件
│   ├── Serialization\ 原 CocoaAssembly\，命名空间统一 .Serialization
│   └── Frontend\      阶段 3 新增：LexerBase/ParserBase/BinderBase/节点描述 + 生成器
├── Cocoa.CodeAnalysis.Cocoa\      ← 原 Cocoa.Core.Cocoa
├── Cocoa.CodeAnalysis.CSharp\     ← 原 Cocoa.Core.CSharp
├── Cocoa.CodeGen.IL\              ← 原 Cocoa.Core.Managed（含下沉的 MetadataBuilder）
├── Cocoa.CodeGen.Native\          ← 原 Cocoa.Core.Native（删遗留 Runtime.X64/X86）
├── Cocoa.CodeGen.PE\              ← 原 Cocoa.Core.PEFile
├── Cocoa.CodeGen.Interpreter\     ← 原 Core\Evaluation\ 独立成后端
├── Cocoa.ProjectSystem\           ← 原 Cocoa.Builder
├── Cocoa.CommandLine\             ← 原 Cocoa.Compiler（AssemblyName=cocoa 不变）
└── Cocoa.Tests\
```

---

## 阶段 0：安全网与仓库卫生 ⬜

| # | 动作 | 状态 |
|---|------|------|
| 0.1 | 黄金基线：扩展 SampleSmokeTests 覆盖 samples 全量样例，断言 IL 与 Native 双后端输出一致 | ✅ 新增 `Golden_DotNet_LibrariesAndClasses_Snapshot`（.coa 库/.NET dll 库/OOP 三项目输出快照）+ `Golden_Native_CodLibrary_Snapshot`（native 下 .coa 库语义一致性）；注意：CSharpClass 的 `Greet()` 输出为 `Hello, Alice`（自动属性 Name 独立后备字段，不共享 _name，与 C# 语义一致） |
| 0.2 | 解决方案收敛：删 `.sln`，`.slnx` 唯一来源，Solution Items 挂 props+targets，build.cmd/sh 改用 slnx | ✅ |
| 0.3 | 删 `Cocoa.Core\Properties\launchSettings.json` 空壳 | ✅ |
| 0.4 | 基线记录：`build.cmd` 全绿，记录测试数 | ✅ **基线：41,821 通过 / 0 失败 / 1m43s（2026-09-03，SDK 10.0.400 / net9.0）** |

## 阶段 1：正确性修复（1a/1b/1c 三个 commit）✅（1a/1b/1c 均已提交，A10/B6/B9 推迟项见进度日志）

代码级审查结论，每条带 文件:行 证据，均为"静默产出错误结果"或健壮性风险：

### 1a Core 正确性
| # | 问题 | 位置 |
|---|------|------|
| A2 | 常量折叠移位掩码 `&63` ≠ 运行期 `&(BitWidth-1)`，编译期/运行期结果不一致；顺手收敛折叠器私有 NumericBox 翻版 | `Cocoa.Core\Binder\ConstantFolding.cs:218,242`（对照 `Evaluator.Members.cs:557`） |
| A1 | 手写 `BoundChildren` switch 漏 Throw/Try/ConstructorChain/ByRef → .coa 库校验、定值分析、单态化、CFG 校验 4 处集体失明；**直接替换为 BoundTreeWalker 实现**（原阶段 4.2 的这项提前） | `Cocoa.Core\Compilation.cs:917-1039` |
| A3 | Emit 不检查 program.Diagnostics 即发射（Evaluate/EmitCocoa 都检查了） | `Compilation.cs:578-602` |
| A6 | NativeCodeEmitter 整条流水线跑两遍（EstimateDataRva 遗留）；`.ir.txt` 无条件落盘，挂环境变量门禁 | `Cocoa.Core.Native\NativeCodeEmitter.cs:22,32` |
| A7 | 后端注册静态字段无 volatile/同步 | `Compilation.cs:28-39` |
| A8 | GenericTypeInstantiator 进程级缓存跨编译泄漏，挂 Compilation 生命周期或键绑定定义身份 | `Symbols\GenericTypeInstantiator.cs:17,44` |
| A4 | LaunchProcess 重定向 stdout/stderr 不读取 → 管道死锁；Process.Start null 检查 | `Evaluator.Calls.cs:317-332` |
| A5 | Evaluator 构造 Dictionary.Add 重复键崩溃 → 改索引器（最新 submission 优先） | `Evaluator.cs:43-54` |
| A10 | mojibake 编码修复：受影响文件实际 109 个（比初判广）；**已推迟**——自动修复试验（GBK 回转+严格 UTF-8 校验）产生半修复混合注释，且有把正确中文误转的风险（损坏时丢失的字节无法恢复）；需专门的手工/半自动批次处理 | 多文件 |

### 1b 前端正确性
| # | 问题 | 位置 |
|---|------|------|
| B1 | foreach 集合表达式不提升临时变量，副作用每次迭代重求值；switch 判别式每 case 重复求值 | `CocoaBinder.Statements.cs:1203,1411,1444`（及 CSharp 对应） |
| B2 | Lambda 块体 return 只扫顶层语句，嵌套 return 推断成 void | `CocoaBinder.Expressions.cs:249-279`（及 CSharp 对应） |
| B3 | 无分号检查 + return 按行判断，跨行 return 静默错切 | `CocoaParser.cs:1839,1827`（及 CSharp 对应） |
| B4 | 插值洞扫描不处理 char 字面量（`$"{ '}' }"` 截断） | `CocoaLexer.Strings.cs:403-452` |
| B5 | `\U` 超范围时位置不推进 → 级联 BadToken | `CocoaLexer.CharsNumbers.cs:75-84` |
| B6 | `_syntheticTokens` 与 Peek(offset) 前瞻不一致（**已推迟至阶段 3** 与 ParserBase 提取一并修） | `CocoaParser.cs:94-118` |
| B7 | 前瞻魔法数 128 截断探测（1b 已修：MaxLookahead=4096） | `CocoaParser.cs:362,1154` |
| B8 | binder 非法语法 `throw new Exception`（含拼写错误 Unexcepted）→ 改诊断（1b 已修：报诊断+降级 Nop/ErrorExpression） | `CocoaBinder.Statements.cs:75,1731` 等 |
| B9 | 四份复制的逗号列表解析合并为 ParseSeparatedList（**已推迟至阶段 3** 去重时合并） | `CocoaParser.cs:718,868,894,1606` |
| B10 | Binder 构造重复（`GetMethod` 单数取第一个；无 ctor 静默跳过参数校验）（1b 已修：按元数在候选集中解析；候选=类名与泛型定义简单名的并集，见进度日志踩坑记录） | `CocoaBinder.Expressions.cs:586-608` |

注意：1b/1c 中 Cocoa 与 CSharp 两份副本都要修（阶段 3 去重前的临时双修）。

### 1c 后端正确性
| # | 问题 | 位置 |
|---|------|------|
| C1 | x86 LeaSlot 帧底缓冲与 x87 转换常量槽重叠（共存即踩踏）（**1c 已修**：帧布局分区——x87 暂存区固定帧底 16 字节，LeaSlot 缓冲经 `_x87ScratchBytes` 上移） | `LirToAssembler.Instructions.cs:354 vs 1576` |
| C2 | IlAssembler MaxStack 表缺 Cgt_Un/Clt_Un；Ret 后无深度钳制（**1c 已修**：净栈 -1 补齐 + Ret 后深度重置） | `IlAssembler.cs:291,195` |
| C3 | ShortInlineBrTarget 静默 sbyte 截断 → 溢出时升级长格式或显式抛错（**1c 已修**：超界响亮抛错；发射侧现无 _S 分支，属防御性自检） | `IlAssembler.cs:395-400` |
| C4 | IlEmitter 隐式 ret 只看顶层最后语句，嵌套块结尾产出无效 IL（**1c 已修**：TailEndsWithReturn 递归下探块/双分支 if/序列点） | `IlEmitter.cs:488-494` |
| C5 | 发射后自检：占位符残留/token 0xFFFFFFFF/槽宽校验（**1c 已修**：ValidatePatched 校验 token/#US 占位符回填；Patch 缺失键已有 throw；槽奇偶校验核实无正确性影响——x86 允许非对齐、双槽连续即正确——跳过） | IlAssembler.Patch / X64Assembler.Patch |
| C6 | native 静态字段每次访问重复 AddData，按 FieldSymbol 缓存（**核实无需修复**：LirProgram.AddData 已按 key 幂等去重，重复调用无害） | `MirToLir.Expressions.cs:222-229` |
| C7 | x86 数据引用未纳入 .reloc（HIGHLOW），不依赖 DllCharacteristics 兜底（**核实已实现**：M4a 全链贯通——DataAbsoluteFixups → BuildRelocBlob（x86 HIGHLOW/x64 DIR64）→ BaseReloc 目录 + .reloc 节，计划写于实现之前） | `X86Assembler.cs:129-171` |
| C8 | ManagedPEWriter stub 硬编码 imageBase 0x400000 双写（**1c 已修**：提取 NetFxImageBase 常量，stub 操作数与 PeImageConfig 同源） | `ManagedPEWriter.cs:146,171` |

## 阶段 2：项目/文件夹/命名空间全面改名 ✅（纯 git mv + 重命名，零逻辑改动）

| 现 | 新 | AssemblyName | 命名空间 |
|----|----|----|----|
| Cocoa.Core | Cocoa.CodeAnalysis | Cocoa.CodeAnalysis | `Cocoa.CodeAnalysis.*`（不变） |
| Cocoa.Core.Cocoa | Cocoa.CodeAnalysis.Cocoa | 同名 | 不变 |
| Cocoa.Core.CSharp | Cocoa.CodeAnalysis.CSharp | 同名 | 不变 |
| Cocoa.Core.Managed | Cocoa.CodeGen.IL | 同名 | `.Emit.Managed` → `Cocoa.CodeGen.IL` |
| Cocoa.Core.Native | Cocoa.CodeGen.Native | 同名 | 不变 |
| Cocoa.Core.PEFile | Cocoa.CodeGen.PE | 同名 | `.Emit.Native.PEFile` → `Cocoa.CodeGen.PE` |
| Cocoa.Builder | Cocoa.ProjectSystem | 同名 | `Cocoa.Projects` → `Cocoa.ProjectSystem` |
| Cocoa.Compiler | Cocoa.CommandLine | **cocoa（不变）** | `Cocoa.Compiler` 保留 |

Core 内部：`MetadataReader\`→`Emit\IL\`；`Binder\`→`Bound\`；`CocoaAssembly\`→`Serialization\`（命名空间统一）；`IO\`并入`Text\`；根目录 `Language.cs`/`SemanticModel.cs`/`MetadataReference.cs` 入 `Compilation\`。
命名空间漂移修正约 5 处（PEFile 内部统一、Repl\Authoring、Tests 的 Lir 补 .Native 段）。
配套更新（7 项）：.slnx、build.cmd/sh、cocoa.cmd/sh、**tools\build-sdk.cmd**（硬编码 Cocoa.Compiler 路径）、Directory.Build.targets、InternalsVisibleTo、ProjectReference。

> **执行情报（2026-09-03 盘点）**
> - 现有 11 工程：上表 8 个 + `Cocoa.Compiler.Cocoa` / `Cocoa.Compiler.CSharp`（计划未列，暂保留原名）+ `Cocoa.Tests`
> - 全部命名空间清单已盘点：`Cocoa.CodeAnalysis.Emit.IL`（被 MetadataReader 文件夹引用！）、`Cocoa.CodeAnalysis.Emit.Managed`、`Cocoa.CodeAnalysis.Emit.Native(.Assembler.X86/.X64/.Lir/.PEFile/.Runtime.Windows.*)`、`Cocoa.CodeAnalysis.CocoaAssembly`、`Cocoa.IO`、`Cocoa.Projects`、`Cocoa.Compiler(.Cocoa/.CSharp)`、`Cocoa.Tests.*`
> - MetadataReader\ 文件夹内的文件命名空间**已是** `Cocoa.CodeAnalysis.Emit.IL` → 纯文件夹移动即可
> - `CocoaAssembly\`→`Serialization\` 的"命名空间统一"含测试侧 `Cocoa.Tests.CodeAnalysis.CocoaAssembly` → `Cocoa.Tests.CodeAnalysis.Serialization`
> - `IO\` 并入 `Text\`：现 ns `Cocoa.IO`，并入后归 `Cocoa.CodeAnalysis.Text`（需全仓 using 更新）
> - **执行手法（本轮验证过的安全做法）**：git mv 后，对全仓 .cs/.csproj/.slnx/.props/.targets/.cmd/.sh 做**字节级 ASCII 替换**（`[System.IO.File]::ReadAllBytes` + 字节模式替换 + 原样写回，纯 ASCII 模式在 UTF-8 中字节透明，绝不经字符串解码往返）；严禁 Get-Content/Set-Content（GBK 吞字节，见阶段 1b 教训）；每步 `--no-incremental` 构建 + 全量测试

## 阶段 3：前端共享与独立（Roslyn 式）✅

> **设计原则澄清（2026-09-03，用户）**：本项目一开始就参照 Roslyn 设计——前端按语言独立
> （Binder/Parser/Lexer/SyntaxFacts/Compilation/SemanticModel 各自成套，CSharp/Cocoa 双份是刻意设计）。
> 阶段 3 原"共享骨架收敛"叙事作废；保留的价值改为"Roslyn 式独立 + 双写漂移防护"。

- ~~新增共享项目 `Cocoa.CodeAnalysis.Frontend`~~：不再需要（3b 共享 LexerBase 落在 Cocoa.CodeAnalysis；3c 取消合并）
- 新语言接入成本 = 复制方言包（93 文件）+ 词汇表/Parser 差异 + 语义分化（Roslyn 式；漂移测试提供同步护栏）
- 3a ~~语法节点 Source Generator~~ → **回退手写双份 + 漂移检测测试**（决策链见 `8e68fbb`）
- 3b Lexer 收敛 ✅（保留，`d4ee651`）：四文件逐字节同仅差类名、内部用共享 SyntaxKind → 共享 `LexerBase` + 两方言薄壳（保留类型身份）；净删 ~3,200 行。将来语言词法真分化时原地分叉或 git revert 一步回退
- 3c ~~BinderBase/ParserBase 提取~~ → **取消**（Roslyn 式按语言独立是设计原则，用户澄清）。改为漂移防护扩展 ✅：
  - `Binder_Files_Stay_In_Sync_Modulo_Prefix_And_Comments`：Binder 5 partial + Compilation + SemanticModel 归一去注释比对（免疫注释语言/位置/风格差异，代码必须一致）；Statements 白名单（ForRange 蓄意分化）
  - `Dialect_SyntaxFacts_Stay_Aligned_With_Shared`：方言 SyntaxFacts（**重新定性：Roslyn 式公开 API 面，非死代码**，撤回阶段 5 处置建议）与共享实现同步
  - Parser/Language 不比对（蓄意独立）
- 新语言接入成本 = 复制方言包（93 文件）+ 词汇表/Parser 差异 + 语义分化（Roslyn 式；漂移测试提供同步护栏）

## 阶段 4：Core 拆解与后端清理 🔄（4.1-4.4 ✅，4.5 待做）

| # | 动作 | 状态 |
|---|------|------|
| 4.1 | `Evaluation\` 迁出为 `Cocoa.CodeGen.Interpreter`，`InterpreterBackend.Register()` 同构注册；REPL/测试经注册表获取 | ✅ `9f2c635` |
| 4.2 | 拆 `Compilation.cs`（950→279 行核心）：NamespaceResolver + AssemblyReferenceManager + EmitPipeline 三 partial | ✅ `ff2cb3e` |
| 4.3 | MetadataBuilder 写侧（3 文件）下沉 `Cocoa.CodeGen.IL`；读侧 MetadataReader 留共享层，经 `IIlRefIssuer` 接口解耦。**执行修正**：IlOpCode/IlTypes/IlInstruction/IlMethodBody 经依赖核实留守 Core（读侧模型引用它们，下沉会造成 Core→IL 反向依赖）；共享模型 IlTypeDef 等抽出 `IlMetadataModel.cs` | ✅ `92a6e17` |
| 4.4 | 删 `Runtime.X64.cs`/`Runtime.X86.cs`（1,405 行），旧白盒测试迁 LIR 管线（NativeEmitTests 重写为双平台 Theory；X86NativeEmitTests 覆盖被承接删除；-11 +4 = 41,821 全绿） | ✅ `eeefe9b` |
| 4.5 | 巨型文件拆 partial：CocoaParser(2,641)、RuntimeEmitterLir.StringsIo(2,220)、CoaSerializer(1,626+1,653) | ⬜ |

## 阶段 5：规范文档化与债务偿还 ⬜（长期）

| # | 动作 |
|---|------|
| 5.1 | `CODING.md`：命名空间↔项目映射、partial 按职责拆分规则、新语言接入流程、NoWarn 棘轮流程；刷新 docs\ 与 tools\udl\ 过期路径注释 |
| 5.2 | 语义债务清单（定夺类，非 bug）：重载计分过粗、CFG 对 try 盲区、诊断无 ID/措辞 6 种变体、非虚方法按 vtable 分派偏离 IL 语义、BuiltinFunctions 三表人肉同步 |
| 5.3 | Nullable 债 866 个逐项目清零（从 Cocoa.CodeGen.PE 开始） |
| 5.4 | x86/x64 Assembler 簿记 ~130 行复制下沉基类；Evaluator 与 ConstantFolding 运算表统一为单一求值核；NamedTypeSymbol 分配热点缓存 |
| 5.5 | 收窄 InternalsVisibleTo |

---

## 进度日志

| 日期 | Commit | 内容 |
|------|--------|------|
| 2026-09-03 | （本次） | 计划文档落库 |
| 2026-09-03 | 阶段0-卫生 | 删 .sln（.slnx 唯一来源，补挂 Build.targets）、build.cmd/sh 切 slnx（SDK 10.0.400 构建验证 0 警告 0 错误）、删 launchSettings.json 空壳 |
| 2026-09-03 | 阶段0-基线 | 全量测试基线：41,821 通过 / 0 失败 / 1m43s |
| 2026-09-03 | 阶段0-黄金基线 | 新增 2 个 Golden 快照测试（dotnet 全量三项目 + native CodLibrary），基线总数 41,823 |
| 2026-09-03 | 阶段1a | A1 BoundChildren 改经 BoundTreeRewriter 单一来源（新 BoundNodeChildren 收集器，未知节点 throw 而非静默空集；漏检的 Throw/Try/ConstructorChain/ByRef/ObjectCreation 全部覆盖；MemberAssignment 增访 Target 对定值分析更正确）；A2 移位掩码 &63→&(BitWidth-1) + Identity 非整数守卫 + 删折叠器私有 NumericBox 翻版；A3 Emit 补 program.Diagnostics 门禁；A4 LaunchProcess 双向排空防死锁；A5 Evaluator 索引器覆盖；A6 Native 单遍生成（删估算遍）+ .ir.txt 挂 COCOA_DUMP_IR；A7 后端注册 volatile + GlobalScope/GlobalNamespace CAS 后重读；A8 泛型实例化缓存挂 ConditionalWeakTable（随 definition 生命周期）。A10 编码修复推迟。41,823 全绿 |
| 2026-09-03 | 阶段1b | B1 foreach/switch 判别式隐藏暂存（集合/判别式只求值一次，双副本）；B2 lambda 返回类型推断递归化 + 块体 return 经 LambdaReturnConverter : BoundTreeRewriter 递归转换（RewriteReturnStatement 升 protected virtual，不下探嵌套 lambda）；B3 裸 return 后跟表达式起始 token 报诊断（CanStartExpression）；B4 字符串洞扫描补 `'` 与 `//` 分支；B5 `\U` 码点>0xFFFF 位置回退修复；B7 while(i<128)→MaxLookahead=4096；B8 语句/表达式分派 default 改报诊断+降级（Nop/ErrorExpression）替代编译器崩溃；B10 构造重载按元数解析（候选=与类同名方法并集：混淆名+泛型定义简单名）。B10 踩坑记录：InstantiatedTypeSymbol.Name 刻意为混淆名（GenericTypeInstantiator.MangledName），且实例化构造器克隆被改名随类名（PopulateMembers nameOverride 是全编译器构造查找约定），遗留 `function 类名()` 式构造器不打 IsConstructor 也不改名——查找须两名字并集；顺带修 ExternalTypeResolver（外部 .ctor 补 IsConstructor 标志、按名字+元数去重以保留重载）。B6（syntheticTokens/Peek 前瞻不一致）、B9（ParseSeparatedList 合并）推迟至阶段 3 与 ParserBase 提取一并处理。过程教训：① MSBuild 增量构建在 stash 往返后可能不重编译（mtime 判定失真），验证必须 `--no-incremental` 或清 bin/obj，本轮多轮二分曾被陈旧二进制污染；② 严禁用 Get-Content/Set-Content 往返 GBK 默认编码改 UTF-8 源文件（会吞掉中文字符串字面量的引号字节），一律用编辑工具；③ 字符串是否编入 DLL 需按字节模式搜（UTF-16 #US 堆，Encoding.Unicode.GetString 有对齐陷阱）。41,823 全绿 |
| 2026-09-03 | 阶段1c | C1 x86 帧布局分区：x87 暂存区（cw 槽 [-fb..+4)、u64→浮点常量槽 [-fb+8..+16)）固定帧底，LeaSlot 缓冲基址 = -fb + _x87ScratchBytes + 槽宽，两特性共存不再互相踩踏；C2 MaxStack 表补 Cgt_Un/Clt_Un 净栈 -1 + Ret 后深度重置（线性扫描按新路径起算）；C3 ShortInlineBrTarget 超界由静默截断改为响亮抛错（发射侧现无 _S 分支，防御性自检）；C4 隐式 Ret 收尾判断改为 TailEndsWithReturn 递归下探（块尾/双分支 if/序列点），嵌套 return 收尾不再误补冗余 Ret；C5 发射后自检 ValidatePatched：token/#US 占位符 0xFFFFFFFF 回填校验挂接 IlEmitter（Patch 缺失键本就 throw；槽奇偶校验核实无正确性影响，跳过）；C8 ManagedPEWriter 提取 NetFxImageBase 常量消除 stub/PeImageConfig 双写。C6 核实无需修复（AddData 已按 key 幂等）；C7 核实已实现（M4a .reloc 全链贯通，计划写于实现之前）。41,823 全绿（含 2 个 Golden 快照，C1 布局改动未影响金标准——现网无两特性共存的发射路径，修复仅对未来组合生效） |
| 2026-09-03 | 阶段2a | CodeGen 三工程拆分：Cocoa.Core.PEFile→Cocoa.CodeGen.PE、Cocoa.Core.Managed→Cocoa.CodeGen.IL、Cocoa.Core.Native→Cocoa.CodeGen.Native（ns 同步 `Emit.*`→`CodeGen.*`）；TargetPlatform/TargetOS/Architecture/CodeBackend 统一归位 Cocoa.CodeGen.PE（漂移修正）；全仓字节级 ASCII 替换 106+ 文件（csproj/IVT/slnx/using）；工具脚本落 %TEMP%（rename2a.ps1 等）。踩坑：① PE 是叶子工程不引用 Core，插 `using Cocoa.CodeAnalysis;` 会 CS0234，需逐文件核查引用关系再补 using；② 限定名 `Cocoa.CodeGen.Native.TargetPlatform` 等跨 ns 引用需二次替换到新 ns；③ using 插入脚本首版有缺陷（`.Replace` 语句失败致 $newText 残留污染 57 文件），git checkout 恢复后重做——批量脚本必须先在副本上验证。41,823 全绿 |
| 2026-09-03 | 阶段2b | Core 三工程改名：Cocoa.Core→Cocoa.CodeAnalysis、Cocoa.Core.Cocoa→Cocoa.CodeAnalysis.Cocoa、Cocoa.Core.CSharp→Cocoa.CodeAnalysis.CSharp；ns 本已正确（盘点情报应验），仅同步 slnx/csproj 引用/IVT/路径 16 文件；全仓无 Cocoa.Core 残留。41,823 全绿 |
| 2026-09-03 | 阶段2c | Core 内部重组：MetadataReader\→Emit\IL\、Binder\→Bound\（均纯移动 ns 不变）；CocoaAssembly\→Serialization\（ns Cocoa.CodeAnalysis.CocoaAssembly→Cocoa.CodeAnalysis.Serialization + 测试目录/测试 ns 同步）；IO\TextWriterExtensions.cs 并入 Text\（ns Cocoa.IO 保留——原计划"归 Cocoa.CodeAnalysis.Text"未做，避免无谓扩散，记为可选项）；根目录 Language.cs/MetadataReference.cs/SemanticModel.cs 入 Compilation\。踩坑：在 ns Cocoa.CodeAnalysis 内引用 Serialization 会撞上方言语法 ns `Cocoa.CodeAnalysis.Cocoa`（首段 `Cocoa` 被就近解析为方言 ns），语言文件改用 `global::` 限定。41,823 全绿 |
| 2026-09-03 | 阶段2d | 收尾：Cocoa.Builder→Cocoa.ProjectSystem（ns Cocoa.Projects→Cocoa.ProjectSystem）、Cocoa.Compiler→Cocoa.CommandLine（**ns Cocoa.Compiler 保留**、AssemblyName=cocoa 不变）；cocoa.cmd/cocoa.sh/tools\build-sdk.cmd 硬编码路径同步（`Cocoa.Compiler` 后无分隔符的场景会被"目录+引用两种语义"模式漏掉，单独补替换）；测试 LirTests ns 补 `.Native` 段（第 5 处漂移修正）。41,823 全绿，阶段 2 完成 |
| 2026-09-03 | 阶段3a | 决策变更：源生成器→模板+脚本→**回退手写双份+漂移检测**（决策链见阶段 3 开头记录；期间完成模板迁移验证了 72/74 对节点逐字节相同、Green 工厂仅差 ForRange 等 3 处入口、kind 枚举仅差 ForRangeStatement=161，为漂移白名单提供了准确边界）。新增 SyntaxDuplicationDriftTests 3 测试（节点归一比对/工厂入口集合等价/枚举值域对齐），负向验证通过（人工注入漂移→红，恢复→绿）。基线 41,826 全绿 |
| 2026-09-03 | 阶段3b | Lexer 收敛：盘点证实 4 文件逐字节同仅差类名且内部用共享 SyntaxKind → 共享 LexerBase（abstract partial，4 文件 ~1,620 行入 `Cocoa.CodeAnalysis\Syntax\`）+ 两方言薄壳（`sealed class XLexer : LexerBase`，保类型身份），净删 ~3,200 行；Language.CreateLexer/IsType 断言/ILexer 消费方零改动。三枚举收口扩展进漂移测试（方言枚举须与共享 SyntaxKind 逐成员同值）。副产品发现：方言 SyntaxFacts 两个 544 行文件是零消费死代码（与共享版相同），留待阶段 5（API 面核查后处置）。41,826 全绿 |
| 2026-09-03 | 阶段3c | 用户澄清设计原则：参照 Roslyn、每语言独立是刻意设计 → 取消 BinderBase/ParserBase 提取；SyntaxFacts 重新定性为 Roslyn 式公开 API（撤回死代码结论）。漂移防护扩展 +2 测试：Binder 五 partial + Compilation + SemanticModel 归一去注释比对（去注释/去 using/去空行，免疫注释语言与位置差异；共享 SyntaxKind 用隐式值风格，解析器支持 `Name,` 递增）；Statements 白名单（ForRange 蓄意分化）；方言 SyntaxFacts 与共享实现同步。负向验证通过（注入漂移→红，还原→绿）。41,828 全绿，阶段 3 收口 |
| 2026-09-03 | 阶段4.1 | Evaluation 迁出：Evaluator 7 文件 → Cocoa.CodeGen.Interpreter（ns 迁移后裸限定名 `Symbols.X`/`Binding.X` 失效——using 别名修复）；EvaluationResult 留守（公开 API）；Core 增 RegisterInterpreterEvaluator 委托（未注册抛 InvalidOperationException）；Program.Main + 测试 ModuleInitializer 注册。41,828 全绿 |
| 2026-09-03 | 阶段4.2 | Compilation.cs 950→279 行 + 3 partial（NamespaceResolver/AssemblyReferenceManager/EmitPipeline），纯移动零逻辑。教训：行号切块脚本的过滤循环必须"基于旧列表构建新列表"（曾因迭代刚清空的列表把主文件清零，git checkout 恢复）。41,828 全绿 |
| 2026-09-03 | 阶段4.3 | MetadataBuilder 写侧下沉 CodeGen.IL。关键发现：Reader 的 FindType/FindMethod/FindField 以 MetadataBuilder 为参数（发射期把解析到的 AssemblyRef/TypeRef 发进写侧表）→ 接口 IIlRefIssuer 反转解耦；IlOpCode/IlTypes/IlInstruction/IlMethodBody 留守（下沉会造成 Core→CodeGen.IL 反向依赖，计划此处修正）；IlTypeDef 等 5 个表行模型抽 IlMetadataModel.cs。41,828 全绿 |
| 2026-09-03 | 阶段4.4 | 删 Runtime.X64/X86（1,405 行手工布局路径，生产已全走 LIR）；NativeEmitTests 重写为双平台管线 Theory（PE 头 0x8664/0x014C + 运行时冒烟含输入回显）；X86NativeEmitTests 白盒覆盖承接删除；`NativeEmitTests.Run` 辅助保留（3 处跨文件复用）。-11 +4 测试 = 41,821 全绿 |
