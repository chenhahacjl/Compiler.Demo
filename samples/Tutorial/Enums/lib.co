public enum Color { Red, Green, Blue }

public enum HttpStatus { OK = 200, NotFound = 404, InternalServerError = 500 }

function IsGreen(c: Color): bool
{
    return c == Color.Green
}

function AsInt(c: Color): int
{
    return int(c)
}