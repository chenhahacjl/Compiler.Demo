function Add(a: f64, b: f64): f64
{
    return a + b
}

function Divide(a: f64, b: f64): f64
{
    return a / b
}

function IsBigger(a: f64, b: f64): bool
{
    return a > b
}

function Truncated(d: f64): i32
{
    return i32(d)
}