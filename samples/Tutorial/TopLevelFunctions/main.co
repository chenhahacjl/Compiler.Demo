// 入口函数
function Main()
{
    print(Add(2, 3))
    print(Add(10, 20))

    var nums = GetNumbers()
    print(nums.Length)
    print(nums[0] + nums[1] + nums[2])

    print(Repeat("ab", 3))

    print(Double(21))
}

// 顶层函数（类型后置 + 冒号返回类型）
function Double(x: int): int
{
    return x * 2
}
