using System
function Main()
{
    Console.WriteLine(Classify(5))
    Console.WriteLine(Classify(0))
    Console.WriteLine(Classify(-3))
    Console.WriteLine(SumUpTo(5))
    Console.WriteLine(Countdown(3))
    Console.WriteLine(DoOnce())

    var total = 0
    for var i = 1 to 5
    {
        if i == 3
        {
            continue
        }
        total = total + i
    }
    Console.WriteLine(total)

    var j = 0
    while j < 3
    {
        Console.WriteLine(j)
        j = j + 1
    }
}