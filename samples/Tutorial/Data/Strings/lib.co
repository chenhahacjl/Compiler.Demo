function Middle(s: string): string
{
    return s.substring(1, 3)
}

function FirstChar(s: string): char
{
    return s[0]
}

function CharCode(s: string, index: i32): i32
{
    return i32(s[index])
}

function LengthOf(s: string): i32
{
    return s.Length
}