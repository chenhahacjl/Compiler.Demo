# 文本文件一律 LF 入库、CRLF 检出（Windows 工作区换行统一为 CRLF）
* text=auto eol=crlf

# 脚本文件保持 LF（Unix 兼容，与 .editorconfig 的 [*.sh] end_of_line=lf 一致）
*.sh text eol=lf

# 二进制文件不进行换行归一化
*.dll binary
*.exe binary
*.png binary
*.jpg binary
*.ico binary
*.pdb binary
*.coa binary
*.stamp binary
*.cache binary
