# Cocoa 开发计划 — 阶段路线图

> 当前阶段：**阶段 6**（IL 路径自研已完成 5-1~5-5；Native 跨 DLL 互操作 6c-1/6c-2 已落地；语言扩展 6a-1（数组）/6a-2（字符串+char）/6a-3（enum）/6a-4（byte）/6a-5（double）已完成；项目系统 6d-1/6d-2/6d-3/6d-4 已落地；语言打磨 6e-M10（C# 兼容语法 + 字段/自动属性初始化器 + .cctor，2026-08-20））
> 相关文档：`docs/实现目标.md` — 架构设计 · `docs/语法手册.md` — 语言参考 · `docs/IR设计.md` — IR 草案
> 最后更新：2026-08-20

---

## 总览

```
阶段 0 ── 阶段 1-3 ── 阶段 4 ── 阶段 5 ── 阶段 6 ── 阶段 7 ── 阶段 8 ── [阶段 9 可选]
修 bug      IR 层       运行时     IL 自研    语言+互操作    co 重写     自举验证      .NET Hosting
（双后端共用） IR 化     （零依赖） +输出+项目    编译器
```

| 阶段 | 名称 | 关键交付 | 前序依赖 |
|------|------|---------|---------|
| 0 | x86 崩溃修复 | 黑盒对照（TinyCC/gcc/objdump）修复 `Runtime.X86.cs` 崩溃 | — |
| 1 | IR 层设计 | 三地址码 + 虚拟寄存器 + IR 打印器 | 0 |
| 2 | Native 后端 IR 化 | 绑定树 → IR → IAssembler（x86/x64 共用） | 1 |
| 3 | IR 语义对齐 | 严格复刻现有 ABI/求值语义，回归全绿 | 2 |
| 4 | 运行时 IR 化 | Input/WriteStr/Alloc/CopyChars/Concat 重写，x86/x64 合并 | 3 |
| 5 | IL 路径自研 | IL 编码器 + ECMA-335 元数据写入器，移除 Mono.Cecil/Mono.Options | 4 |
| 6 | 语言+互操作+输出+项目 | 见 §6 | 5 |
| 7 | 自举 | 编译器用 Cocoa 语言重写（Native+IL 全自举） | 6 |
| 8 | 自举验证 | Stage0/1/2，B1 ≡ B2（二进制或行为等价） | 7 |
| 9 | （可选）Native .NET 互操作 | CLR Hosting：Native 路径托管加载 .NET 程序集 | 8 |

---

## 阶段 0 — 修复 x86 Native 崩溃 ✅ 已完成（2026-08-13）

**目标**：修复 hello-x86.exe 在管道输入 "123\r\n" 时的崩溃（0xC0000005）。

**方法**：黑盒对照 —— 不靠猜测，用等价 C 程序 + 成熟编译器验证行为。

| 步骤 | 动作 |
|------|------|
| 0.1 | 安装 TinyCC + MinGW-w64（含 objdump） ✅ |
| 0.2 | 编写与 Runtime.X86 语义等价的 C 程序（input/print/字符串处理） ✅ |
| 0.3 | `objdump -d -Mintel` 对照反汇编：栈帧、栈检查、input 路径、字符串布局、调用约定、导入表 ✅ |
| 0.4 | 同输入 "123\r\n" 运行 tcc/gcc 版本，确认行为 ✅ |
| 0.5 | 修复 `src/Cocoa/CodeAnalysis/Emit/Native/Runtime/Windows/X86/Runtime.X86.cs` ✅ |
| 0.6 | 回归：runx86.ps1 exit=0、stdout 完整；x64 同步验证 ✅ |

**验收标准**：hello-x86.exe 管道输入不再崩溃；x86/x64 双平台基线测试全绿 ✅。

### 阶段 0 结论（根因与修复记录）

**崩溃链**：`main → print → WriteStr(0x401530) → call [0x404094]`。IAT 槽 0x404094 被自举导入 stub
误填为 `WriteConsoleA` 的真实地址（0x766DF2F0，应为 WriteConsoleW 0x766DF300），跳到错误入口执行
垃圾代码，最终落入 kernel32 导出名字表（PAGE_READONLY 数据页）→ 取指访问违规 0xC0000005。

**根因**：`ImportResolverStubEmitterX86.AnsiWords()` 只用导出名的**前 8 字节**生成匹配模板，
名字超过 8 字符时第 9 字节起未进入模板。kernel32 名字表按字母序扫描，前 8 字节相同的变体先被命中：
`GetFileType→GetFileTime`、`ReadConsoleW→ReadConsoleA`、`WriteConsoleW→WriteConsoleA`。
`WriteFile` 恰好正确——它是 `WriteFileEx`/`WriteFileGather` 的最短前缀，字母序最前先被扫到。
（x64 版同模式缺陷，16 字节阈值，当前最长导入名 15 字符侥幸未触发。）

**修复**（2026-08-13，双平台对称）：
- `src/Cocoa/CodeAnalysis/Emit/Native/Runtime/Windows/X86/ImportResolverStubEmitterX86.cs` — `AnsiWords`
  改为按名字实际长度动态生成全部 dword 模板，Emit 循环逐块比较，掩码屏蔽跨界读入的相邻名字字节
- `src/Cocoa/CodeAnalysis/Emit/Native/Runtime/Windows/X64/ImportResolverStubEmitter.cs` — 相同修复（qword 粒度）

**验证**：
- hello-x86/hello-x64 控制台模式正常输出、exit=0；`debughost6` 确认 IAT 槽全部与导出表精确一致
- `ImportResolverTests` 17 个回归测试（模板全长度覆盖 + 前缀变体扫描模拟），4877 测试全绿

---

## 阶段 1-3 — IR 层

**目标**：在绑定树（Lowerer 输出）与 IAssembler 之间插入统一 IR，使 x86/x64 后端共享同一中间表示。

### 阶段 1：IR 设计

- 三地址码（每个指令最多一个目的寄存器 + 两个操作数）
- 虚拟寄存器（无限，由后端分配物理寄存器/栈槽）
- 逻辑/比较直接写入目的寄存器（`cmp v_dst, v_a, v_b`）
- IR 指令集覆盖：常量加载、mov、算术/逻辑/比较、分支/标签、call、栈操作、内存读写（字符串/数组）
- IR 打印器（文本形式，用于测试与 .cod 输出）

**阶段 1 已完成（2026-08-13）**：`Emit/IR/` 骨架（IrVirtualRegister 分配器/IrOpCode/IrInstruction/IrCond/IrProgram/IrPrinter）
落地，采纳「无限虚拟寄存器 + 三地址码」模型（详见 `docs/IR设计.md` §9）；`IrTests` 14 个测试，全量 4891 绿。

### 阶段 2：Native 后端 IR 化

- 新增 IR → IAssembler 后端（x86/x64 共用）
- `NativeCodeEmitter` 改为绑定树 → IR Lowerer + IR → IAssembler
- 严格复制现有 ABI：调用约定、栈布局、参数传递、返回值（eax/rax）语义

**阶段 2 已完成（2026-08-13）**：`BoundTreeToIr.cs` + `IrToAssembler.cs` 落地；
NativeCodeEmitter 重写为薄壳走 IR 管线；x64 对齐补丁并入 ReserveArgs/FreeArgs 配对（嵌套调用参数区错位修复）。
Native 测试 83/83 通过。

### 阶段 3：语义对齐

- 与现有 x86/x64 输出逐指令对照（同一 .co 文件双后端输出相同语义）
- 既有 Native 测试全部保持绿色
- 为阶段 4 的运行时 IR 化提供基础

**阶段 3 已完成（2026-08-13）**：双平台 NativeSourceEmitTests 全过；全量 4901 绿；
TwoInput.co 管道输入 "123\r\n"（阶段 0 崩溃场景）x86/x64 均 exit=0 且输出一致。

**验收标准**：IR 层就绪，Native 输出与当前实现行为一致；x86 崩溃测试通过。

---

## 阶段 4 — 运行时 IR 化 ✅ 已完成（2026-08-13）

**目标**：把内建运行时函数（Runtime.X86.cs / Runtime.cs 中的手写汇编/托管实现）统一为 IR 生成。

| 顺序 | 函数 |
|------|------|
| 1 | `Input`（崩溃相关，优先） |
| 2 | `WriteStr` |
| 3 | `Alloc` / `CopyChars` / `Concat` |
| 4 | 其余（print/exit/random 等） |

- x86/x64 运行时合并为一份（平台差异收敛到 IR 指令选择层）
- **验收标准**：Runtime 双平台单实现，全部测试通过。

**阶段 4 已完成（2026-08-13）**：`RuntimeEmitterIR.cs` 单份实现覆盖全部 17 个运行时函数
（WriteStr/BuildInt/Alloc/CopyChars/PrintString/PrintInt/IntToString/ParseInt/ParseBool/Concat/StrEquals/Input/
Random/ObjectEquals/ExitProcess/DivByZero/StackOverflow），`IrToAssembler` 平台差异收敛到指令选择；
双平台 NativeSourceEmitTests 42/42 全过，全量 4901 绿。

### 阶段 4 结论（关键修复记录）

运行时 IR 化过程中暴露并修复了 10 个编译器后端缺陷（部分为 IR 化前就潜伏的既有 bug）：

| # | 根因 | 修复 |
|---|------|------|
| 1 | SSA 循环内变量重绑定（`tail = nextTail`）不生成复制指令——IR 线性复用导致每次迭代读初始槽 | 循环内改显式 `Mov(tail, nextTail)` 回写（BuildInt/CopyChars/ParseInt/StrEquals） |
| 2 | 分支内重绑定 + 分支外使用（`tail = signTail`）：无符号路径槽未初始化读垃圾 | φ 展开：两条路径统一赋值 `copyTail`（BuildInt） |
| 3 | `LeaSlot` 缓冲越界：buf 槽距返回地址 0x28 而 BuildInt 需 buf+44，写穿返回地址 | frame 预留 0x80 缓冲 + LeaSlot 指向帧底（x64/x86 对称） |
| 4 | x86 `EmitSetArg` 对第 3 参数直接跳过（`ordinal>=2` return），CopyChars 的 count 丢失 | 第 3 参数发射到 ESI（与 EmitInitRegArg 一致） |
| 5 | `EmitSysCall` 第 5 参数固定写 0，Input 的 writtenAddr 丢失 | 第 5 参数从 `_sysArgs[4]` 取值 |
| 6 | `EmitUdiv` 缺除零检查（Idiv 有）→ 硬件除零异常 | 补 `cmp ecx,0; je DivByZero` |
| 7 | 16 位 `Load` 用 `mov ax`（66 8B）不清高位，字符比较被高位垃圾污染 | 16 位加载改 `movzx`（0F B7） |
| 8 | DivByZero/StackOverflow 由 jcc（不压栈）进入，入口 rsp 与 call 进入差 8，函数内对齐错 → 调 kernel32 时 KERNELBASE movdqa 崩 | 这两个函数帧大小 ≡8（先对齐 ≡0 再 +8，仅按函数名判定） |
| 9 | Input 的 `LeaSlot(writtenAddr)` 在 console 分支内赋值，notConsole 分支用未初始化槽 | LeaSlot 移到 GetFileType 判断前的公共路径 |
| 10 | 探索过程中的教训：构建失败时 dotnet test 静默用旧 dll，产生"全崩"假象 | 修改后端后先显式 `dotnet build` 确认编译通过再跑测试 |

调试工具链：procdump（Sysinternals）抓崩溃 dump + cdb（WinDbg 包 `amd64\cdb.exe`）分析/软件断点二分。

---

## 阶段 5 — IL 路径自研（零第三方依赖）

**目标**：移除 Mono.Cecil 与 Mono.Options，IL 路径完全自研。

| 组件 | 说明 |
|------|------|
| IL 编码器 | 指令字节序列生成（opcode + 操作数 + 分支 fixup） |
| ECMA-335 元数据写入器 | 表、堆（#Strings/#Blob/#GUID）、AssemblyRef/TypeRef/MethodDef 等 |
| 托管 PE 写入器 | 复用 PEFile 基础设施（CLR 头 + 数据目录） |
| Emitter 重写 | 现有 Mono.Cecil 调用全部替换 |

- 仅保留 `System.Collections.Immutable` 依赖
- **验收标准**：IL 路径输出经 `dotnet` 运行验证，互操作测试全绿；csproj 中第三方包清零。

### 阶段 5 里程碑记录（2026-08-14 ~ 2026-08-15，全部已提交）

| 里程碑 | 提交 | 内容 |
|--------|------|------|
| 5-1 | `3b10207` | 自研 IL 核心：编码器 + ECMA-335 元数据读写 + 托管 PE 写入器 |
| 5-2 | `3670f20` | MaxStack 动态计算 + 用户函数/控制流 e2e + CLI 冒烟 + 注释清理 |
| 5-3 | `a6e0a52` | 元数据写侧 P/Invoke：ModuleRef/ImplMap 表 + PInvokeImpl 标志 + 调用约定编码 |
| 5-4 | `9674c6f` | 语言侧 P/Invoke：`import` 子句 + `cdecl`/`stdcall` 外部函数声明 + Binder/诊断/Emitter |
| 5-5 | 本次 | 带参数 extern 端到端验证（ExitProcess/GetModuleHandleW）+ 互操作文档更新 |

**运行时结论（5-4 踩坑记录，勿回退）**：
1. extern 方法的 IL 签名必须用默认调用约定（首字节 0x00）；本机约定（cdecl/stdcall）只由 ImplMap 的 MappingFlags 表达。写 0x01/0x02 会 `TypeLoadException: The signature is incorrect.`
2. extern 方法 MethodDef 行的 ImplFlags 必须写 `PreserveSig`（0x0080），否则 JIT 桩对返回值处理异常（int 返回恒得 0）。
3. MethodDef 行字段顺序为 RVA(4) + ImplFlags(2) + Flags(2)：PInvokeImpl(0x2000) 属 Flags；ImplFlags 与 Flags 不可混淆。

---

## 阶段 6 — 语言扩展 + 互操作 + 输出格式 + 项目系统

### 6a. 语言功能扩展

| 特性 | 说明 |
|------|------|
| `record` | 值/引用语义记录，自动 Equals/ToString 等 |
| 动态数组 | 长度运行时可变（区别于固定数组） |
| `byte[]` | 字节数组类型（互操作基础）✅ 已实现（6a-4） |
| 字符串增强 | 索引、子串、比较运算 ✅ 已实现（6a-2） |
| `enum` | 枚举类型 ✅ 已实现（6a-3） |
| `double` | 浮点类型全链路 ✅ 已实现（6a-5） |
| C# 兼容语法 | 成员双语法/字段初始化器 ✅ 已实现（6e-M10）；顶层函数/表达式体/插值/foreach/switch 🔧 规划（6e-M11，见 §6f） |
| 多文件模块 | 文件间符号可见性管理 |
| 泛型 | 函数/类型级泛型 |
| `import` / `export` | 见 6c/6f |

### 6b. 输出格式系统（见 `docs/输出格式.md`）

```
coc build -p foo.coproj -f exe|dll|cod
```

| 格式 | 说明 |
|------|------|
| `exe` | 现状（Native/IL 可执行文件） |
| `dll` | 写侧导出表 + 重定位表（x86 HIGHLOW / x64 DIR64），`export fn` 标记导出 |
| `cod` | IR 序列化（阶段 1 的 IR 文本/二进制格式） |

### 6c. 互操作（见 `docs/互操作手册.md`）

| 目标 | 路径 | 位置 |
|------|------|------|
| native DLL | `import kernel32.dll` → 导入表消费（与现状 exe 同机制） | 阶段 6 |
| .NET DLL | IL 路径：AssemblyRef 直通 | 阶段 6 |
| `.cod` 库 | IR 加载合并编译（依赖清单：.NET 程序集引用 + native 导入传递） | 阶段 6 |
| Native 路径 .NET | CLR Hosting | 阶段 9（可选） |

### 阶段 6 里程碑记录

| 里程碑 | 提交 | 内容 |
|--------|------|------|
| 6c-1 | 本次 | Native 路径跨 DLL 导入 e2e：多 DLL 导入表 + 启动 stub 模块走查（PEB InLoadOrderModuleList UTF-16 名匹配）→ LoadLibraryA 回退，x86/x64 对称（x86 16 位长度校验修复）；print 补 CRLF 与语言语义对齐（解释器/IL 一致）；`NativeImportE2eTests` 8 测试全绿；互操作文档更新 |
| 6c-2 | 本次 | Native 导入改 OS 加载器静态解析：删除自研 `ImportResolverStubEmitter`/`ImportResolverStubEmitterX86`（含模板测试）；单一 IAT 迁至 data 区，描述符 FirstThunk 指向 data 槽由加载器启动填充——槽磁盘初值须为 hintname RVA（fake-IAT 惯例，0 会被 loader 视为已填充而跳过）；入口即 `main`；`NativeImportE2eTests` 8 测试 + 结构回归 2 测试（`NativeImportStaticResolutionTests`）全绿，全量 5934 测试通过 |
| main-1 | 本次 | main 签名优化：支持 `main()`（默认返回 0）与 `main(): int`（返回值即进程退出码）；managed 统一 `static int Main()`，native 入口 `int main` 的 EAX 返回值转入 ECX 再 ExitProcess（修复恒 exit(0) 隐藏 bug，x86/x64 对称）；`main(): int` 缺 return 保持报错（与其他非 void 函数一致）；数组版 `main(string[] args)`（含 args 两态）待数组类型实现后落地 |
| 6a-1 | 本次 | 数组类型 `T[]` 全链路落地：语法（`new T[n]` / `new T[] {…}` / `new T[n] {…}` / 索引 `a[i]` / 成员 `a.Length`，TypeClause 递归）；绑定（元素类型 int/bool/string，初始化列表长度校验，同型数组赋值）；解释器完整求值；IL 发射（Newarr/Ldelem/Stelem/Ldlen，string[] 走 Ref 变体，修正 Stelem_I4=0x9E 与 Ldelem_Ref=0x9A 编码、补齐 Ldelem_I1/U1/Stelem_I1）；native（长度头 + 8 字节对齐元素区、NewArray/ArrayBoundsCheck 运行时函数、越界打印错误退出码 1，x86/x64 对称）；`AnnotatedText` 支持 `[[`/`]]` 转义（标注 span 偏移 → `AssertDiagnostics` 可选 `assertLocation`）；修复 `EmitNative` 未经 `program.Diagnostics.HasErrors()` 拦截导致 codegen 遇错误类型崩溃；`ComputeMaxStack` InlineType 净增量修正为 0（Newarr/Box 弹 1 压 1）；交错/多维数组明确拒绝；全量 6516 测试通过 |
| 6a-2 | 本次 | 字符串增强 + `char` 类型全链路（解释器 + IL + native x86/x64）：`char` 字面量与转义（`\n` `\t` `\\` `\'`）；转换 `char→int` 隐式 / `int→char` 显式 / `char→string` 隐式；`s[i]` 返回 char（managed「重载 + callvirt String.get_Chars」/ native 长度头 + 2 字节载入 + 越界检查）；`s.Length`；**成员方法语法** `s.substring(start, count)`（新语法节点 `MemberCallExpression` + Parser 提升 + Bound 节点/Printer/Rewriter/三发射器；native 运行时 `Substring`：Alloc+CopyChars+参数校验，非法打印 `error: invalid substring arguments` 退出 1）；`char[]` 数组（IL 补 `Ldelem_U2`=0x93/`Stelem_I2`=0x9D，**修正 Stelem_I2 误注册 0xA5=stelem.any 导致运行时 Bad class token**；native 元素尺寸 2）；string 索引赋值绑定报错（新诊断 `ReportStringIndexNotAssignable`）；`UnknownMember` 消息更新（array/string Length）；IR/IL 常量发射补 char；`IlE2eTests.EmitAndRun` 补 stderr 重定向 + 30s 等待修复并行下异常退出子进程悬挂；新增测试（解释器 9 + IL 3 + native 4×2 平台）；全量 6798 测试通过 |
| 6a-3 | 本次 | enum 枚举类型全链路（解释器 + IL + native x86/x64）：词法（`enum`/`public` 关键字）+ 语法（`EnumDeclarationSyntax`/`EnumMemberSyntax`，逗号分隔、尾逗号、`public` 可选）；`EnumTypeSymbol`（`SymbolKind.Enum`，成员名→int 常量表）注册进 `BoundGlobalScope.Enums`/`CreateParentScope`（跨函数可见）；成员值：显式整数字面量（含负值）/隐式前一成员+1；**独立类型语义**：`Color.Red` 折叠为带枚举类型的常量字面量（`BoundLiteralExpression` 新增显式类型构造器 → 解释器常量短路零成本）、同型 `==`/`!=`（`BoundBinaryOperator.Bind` 动态合成）、`int(c)`/`Color(5)` 双向显式转换（`Conversion.Classify`）、隐式 int→枚举拒绝（提示 cast）；底层 int32 表示：IL（`ToIlType`/`EmitConstantExpression` Ldc_I4/box `System.Int32`/Newarr name 映射）、IR（`ElementSize` 4/print 走 `PrintInt`/转换无操作）、Evaluator（转换无操作）；诊断：成员未定义/成员值非 int/重名复用；新增测试（解释器 12 + IL 2 + native 2×2 平台，LexerTests 关键字自动覆盖）；全量 7361 测试通过 |
| 6a-4 | `6b1c340` | `byte` 类型 + 十六进制字面量全链路（解释器 + IL + native x86/x64）：词法（`0x`/`0X` 十六进制 int 字面量，任意数字数量，非十六进制数字报 `InvalidToken`；`byte` 关键字）；绑定（`LookupType` 注册、`byte → int` 隐式零扩展 + `int → byte` 显式截断（低 8 位）+ `byte → string` 隐式、`byte` 常量越界（>255 或缺省负值）绑定期诊断 `ReportByteLiteralOutOfRange`、`= 300` 类字面量目标类型检查）；`byte[]` 数组（`ElementSize` 1、native 索引读零扩展 movzx、`NewArray` 运行时分配）；IL（`Ldelem_U1`=0x91/`Stelem_I1`=0x9C/`Conv_U1`=0x6D/box System.Byte/Newarr 映射）；EmbeddedText 的 `[[`]]` 转义支持（标注 span 偏移）；`AnnotatedText` 全量走合成 token 持久化构造（消除 Kanji 混入）；修复 `X64Assembler.Movzx` **1 字节零扩展 opcode 错用 0xB7（读 2 字节）→ 0xB6**（byte 读残留相邻字节 65480 类 bug）+ x86 对称修复；修复 `TypeSymbol.ArrayOf` **并发字典双写（构造器内 mutation + 缓存读）**；新增测试（解释器 13 + IL 2 + native 2×2 平台 + LexerTests 自动覆盖 `byte`）；全量 7375 测试通过 |
| 6a-5 | 本次 | `double` 浮点类型全链路（解释器 + IL + native x86/x64）：词法（`.` 小数 `DoubleToken`，`NumberStyles.Float` 解析，非法报 `InvalidNumber`）；绑定（`TypeSymbol.Double`、`int/byte → double` 隐式 + `double → int/byte` 显式截断、double 一元负、`+ - * / == != < <= > >=` 双操作、`string + double` 拼接（运行期转文本后 Concat，常量不折叠保证双后端一致））；IL（`IlType.Double`=System.Double、Ldc_R8/Conv_R8/Add~Div/Neg/Clt/Cgt、double[] Newarr/Ldelem_R8/Stelem_R8、print 走 `Console.WriteLine`）；native（FConst/FMove/FBinary/FNeg/FCompare IR 指令 + SSE movsd 双 32 位槽装载：x86 槽宽 4 高 dword 在低地址槽 `-4`、x64 槽宽 8 高 dword 在槽内 `+4`）；`DoubleToString` 运行时 IR（拆位、隐式高位、定点 6 位舍入剪零、整数去小数点、`Infinity`/`NaN`/`-0/0` 特殊值与零分支；修复零分支 fall-through 到 special 覆盖返回值 + 固定字符串 Alloc 尺寸与 CopyChars dword 数公式）；`MovdGprToXmm` 两架构统一 MOVD `66 0F 6E`、LoadSlotXmm/StoreSlotXmm 双 32 位通用化；修复 x86 `EmitMov` 8 字节拷贝只搬低 32 位等后端缺陷；新增测试（解释器 4 + IL 1 + native 4×2 平台）；全量 7916 测试通过 |
| 6d-1 | 本次 | 项目系统全链路落地（见 `docs/项目格式规范.md`、`docs/编译手册.md`）：`.coproj`/`.cosln` 轻量文本格式解析器 `ProjectFileParser`（顶层 `key = value` + `[sources]`/`[references]`/`[imports]`/`[options]` 分节，字段 `name/output/platform/entry/incremental/debug/outputPath`，非法值/缺 `[sources]`/缺 `[projects]` → `ProjectFileFormatException` 含行号）；`Glob`（`*`/`?`/`**` 转正则展开，未匹配模式警告）；`BuildCache` 缓存**集中到仿 `.vs` 的 `.coc/` 状态目录**（源文件内容 SHA-256 + 引用文件元数据 + imports + 选项快照 → 指纹；解决方案级 `<solDir>/.coc/<项目相对路径>/<name>.cache`、单项目 `<projectDir>/.coc/<name>.cache`、越界路径回退项目名防同名冲突；产物 exe 仍按 `outputPath` 输出，与缓存解耦）；`ProjectBuilder`（Native/DotNet 双后端经 `-backend` 选择（默认 native）、references 存在性/`.cod` 引用未实现校验、dll/cod 输出与 `[imports]` 明确报错占位、输出目录自动创建；修复 `outputPath` 未建目录导致 `PefileWriter` 抛 `DirectoryNotFoundException`）；`SolutionBuilder`（`[references]` 中对 `.cod` 产物的引用建依赖图，Kahn 拓扑排序 + DFS 环检测报告环路径）；`.coproj.user` 用户级覆盖（仿 `.csproj.user`，gitignored 不入库：仅覆盖顶层属性 + `[options]`；`[sources]/[references]/[imports]` 与未知节/未知键一律忽略，为将来 IDE 预留扩展位；优先级 `.coproj` < `.user` < CLI 参数；`.cosln.user` 列后续预留）；CLI `coc build` 子命令（`-p/-f/--platform/-o/-r/-backend/--no-incremental/--debug/--release`，非 coproj/cosln 报错）；测试新增 63 用例（Parser 22 + UserFile 3 + Glob 11 + Cache 10 + SolutionBuilder 5 + BuildCli 12，build-cli 为 e2e：进程内构建 + 子进程运行产物校验退出码，运行目录改唯一 Guid 消除并行/重复运行冲突）；样例重建为 `samples/DemoSolution/`（App + Util 双项目 + 解决方案 + 本地 `App.coproj.user` 演示），清除旧 samples（旧 MSBuild 解释器 harness `Directory.Build.*`/`Hello`/`TwoInput`）；全量测试通过 |
| 6d-2 | 本次 | `main(string[] args)` 命令行参数全链路（解释器 + IL + native x86/x64）并接线 `entry` 字段：Binder 主签名校验放行单 `string[]` 参数（仍限 void/int 返回），诊断文案更新；`entryPointName` 贯穿 `Compilation.Create(entryPointName, …)`/GlobalScope（默认 `main`）；解释器 `Evaluate(args, variables)` 把 argv 绑定为入口函数参数；IL 路径 CLR 自动注入 argv（任意名入口，方法签名含单 `string[]` 即注入）；native 新增 `BuildArgs` 运行时（`GetCommandLineW` + MS 风格引号解析：引号包裹的空白不出界、引号剥离、空 token 保留，构造 `string[]`：计数 → NewArray → 逐 token Alloc+拷贝）供入口函数参数初始化，`BoundTreeToIr` 对入口函数参数改发 `Call BuildArgs`（跳过 ABI InitParam）；修复 `EmitBuildArgs` 循环结构 bug（token 计数寄存器被放循环内反复重置、引号内空白漏计数）；`ProjectBuilder` 用 `project.Entry ?? "main"` 选入口；修复 `ProjectBuildOptions.Backend` 可空化（未指定时不再用 CLI 默认覆盖，语义正确）；新增测试 12 用例（解释器 3 + native/dotnet e2e 4 + entry 项目 2 + IL e2e 2 + main 签名负例）；全量 7991 测试通过 |
| 6d-3 | 本次 | 样例重建为 `samples/Tutorial/`（每功能块一个 exe，全部按已实现语法编写）：`HelloWorld`/`Types`/`ControlFlow`/`Functions`（`entry = run` + `BuildArgs` 命令行参数演示，`args.Length` 守卫避免越界）/`Arrays`/`Strings`/`Enums`/`ByteArrays`/`Doubles`（覆盖 double 比较非折叠路径）/`Interop`（`import kernel32.dll` + `stdcall GetTickCount`），各块 `Block.coproj` + `lib.co`（功能函数）+ `main.co`（入口）；文档同步：`README.md`/`编译手册.md`/`项目格式规范.md` 路径更新为 `samples/Tutorial/`；修复 `编译手册` §9.3 两个错例（`for (var i = 0 to 5)` → `for i = 0 to 5`、while 体内 `;` 分隔无效语法）与 §9.5（`export function` 未实现 → 改真实 `import` + `stdcall` 示例，注明导出待实现），剔除旧的 `samples/DemoSolution`（App + Util）；native 后端修复 `IrToAssembler.MapCond` 缺 `IrCond.Parity/NoParity` 映射（double 非常量比较路径 `Setcc(NoParity)` 此前抛 `Unknown IR cond`，被常量折叠掩盖）；新增 `SampleSmokeTests` 冒烟测试 2 用例（native + dotnet 双后端：构建解决方案 → 逐块运行 10 个 exe 断言输出 → 二次 build 断言 up-to-date → Functions 带参/无参两路径）；`.gitignore` 增 `*.runtimeconfig.json`（IL 后端产物）；全量 7993 测试通过 |
| 6d-4 | 本次 | CLI/coproj 参数重构 + .NET 目标框架（TFM）选择 + 元数据修复：CLI 统一长形式参数（`--path`/`--format`/`--output`/`--reference`/`--backend`，短别名 `-p/-f/-o/-r/-b`），移除旧参数（`-m`/`-target`/`-backend` 旧短形式、`--format` 旧值 `exe/dll/cod`），`--format` 新值 `executable/library/cocoa`（与 coproj `output` 字段一致）；`--dotnet-runtime <TFM>`（`net9.0` 默认/`net8.0`/`net4x`）+ coproj 顶层 `dotnetRuntime` 字段 + `.coproj.user` 覆盖；`--dotnet-module` 模块名（仅 dotnet）；两条命令 `--backend` 默认统一为 `dotnet`（与 `--dotnet-runtime net9.0` 默认自洽）；`IlTarget`（TFM 解析 net<X.Y>→netcore、net4x→netfx）贯穿 `Compilation.Emit`/`Emitter.Emit`/`WriteRuntimeConfig`（netcore 由 TFM 生成 `tfm: net<X.Y>` + `Microsoft.NETCore.App <X.Y>.0`）；元数据修复：`MetadataReader` 编码索引宽度 SUM→MAX（ECMA-335 II.24.2.6，修复读 mscorlib 崩 `IndexOutOfRange`）、mscorlib Assembly 表 PublicKey 计算 PublicKeyToken（`b77a5c561934e089`，ECMA 占位公钥特判 + SHA1 末 8 倒序）、`MetadataBuilder` 堆 0 号预置（`GetOrAddString("")`/`GetOrAddBlob(empty)` 返回 0）、Assembly 表 HashAlgId 修正（0x0804→0x8004）、`#~` Sorted bitmask 修正为 csc/ilasm 确切值 `0x000016003325FA00`（CLR 4.8 对带 native 导入镜像校验该掩码，错误值报 `0x80131018`）；`random()` 改 `newobj Random::.ctor` + `callvirt Next(Int32)`（双运行时兼容）+ 修复 `ComputeMaxStack` 的 `Newobj` 栈深度（.ctor 返回 void 但 newobj 压入实例，此前低估 maxstack 导致 `InvalidProgramException`）；**netfx 直接运行（--dotnet-runtime net4x）已实现**：I386/PE32（AnyCPU）+ mscoree.dll!CorExeMain 导入表内嵌 .text（外部 IAT 槽前置）+ 入口 stub `jmp dword ptr [IAT]` + `.reloc`（特性必须 `0x42000040` Discardable 不可写，可写 `0xC0000040` 同样报 `0x80131018`）+ ASLR，Windows 直接激活 .NET Framework CLR，`x.exe` 双击/直接运行无需 dotnet 前缀，无需 runtimeconfig；`SolutionBuilder` 透传 `DotnetRuntimeOverride`（此前解决方案构建漏传导致回退 corelib 引用）；新增 `NetFxEmitTests` 回归测试 3 用例（直接运行 + `Assembly.LoadFile` + `main(string[])` 命令行参数，全部经 Windows PowerShell 5.1/CLR 4.8 验证）；全量 7996 测试通过 |
| 6e-M10 | 本次 | C# 兼容语法 + 字段/自动属性初始化器（+ .cctor）：**成员双语法**（Cocoa `name: Type` 类型后置 ↔ C# `Type name` 类型前置，同一类内可混用）覆盖字段/方法/属性/构造函数/参数/局部变量；语法树 keyword token 可空化（`function`/`property`/`constructor`/`VariableDeclaration.keyword`/`TypeClause.colonToken`，SourceGenerator GetChildren 只走 SyntaxNode 派生属性不受影响）；Parser 类成员双语法分派（2-token 前瞻、类名下传判定 C# 式构造函数 `ClassName(`）、`ParsePrefixTypeClause`（`int`/`int[]`）、双参数 `name: Type`/`Type name`、访问器 `get; set;` 分号、C# 式局部变量 `int x = 10;`、语句边界分号可选、C# 式接口成员；`LookupType` 补 `void`；**字段/自动属性初始化器**：实例字段注入每个实例构造（`Binder.cs` 构造链 base/this 之后、函数体之前）；静态字段 → 合成 `.cctor` `FunctionSymbol{IsConstructor+IsStatic}`；`Emitter.cs` 名 `.cctor`/declaringType 改 `ContainingClass != null` 判据（**修复既有静态方法/构造归属 Program TypeDef 元数据 bug**，否则 CLR 找不到类的 .cctor）、`MetadataBuilder` `.cctor` 加 `SpecialName\|RTSpecialName`；**修复显式构造无显式链时缺隐式 `base()`**（基类有 0 参构造则注入，保证继承下字段初始化顺序 base→字段→体）；新增测试 28 用例（Parser 12 + Binder 8 + IL e2e 7 + 库反射回归 2：静态方法归属/`.cctor` 存在性）；全量 21284 测试通过；文档：`语法手册.md` §44 C# 兼容语法（对照表/初始化器/.cctor/混用示例/边界）+ §14/§16 同步 |

### 6f. C# 兼容语法补全（规划 6e-M11，P1~P7，Lambda 后置）

> 状态：规划中（2026-08-20，新 session 接续）。目标：在 6e-M10 双语法基础上补齐高频 C# 构型。
> 范围：① 原规划五件套（顶层 C# 函数/表达式体/字符串插值/foreach/switch）；② **已实现但缺 C# 写法**的 6 项清单（const 局部 / using 分号 / 类多接口 / 访问器修饰符 / 静态构造 / 顶层函数，其中顶层函数即原 P1）。
> 核心策略：全部**降级到现有 Bound 节点** → 三条后端（解释器/IL/native）**发射器零改动**，延续"语法层扩展"模式。
> 设计决策（已确认，勿回退）：
> - `=>` 令牌由成员上下文（表达式体）与表达式上下文（Lambda）共享，解析按位置消歧，互不依赖 → **表达式体先行，Lambda 单独后置（P8）**，避免闭包三后端大工程阻塞小步项
> - `foreach` 降级层做成**可扩展策略点**：v1 数组走索引循环；后期换枚举器模式（`GetEnumerator()/MoveNext()/Current`，非泛型 IEnumerable 可走现有 .NET 互操作）；泛型集合待泛型落地（P8+）
> - 静态构造复用既有 `.cctor` 管道：用户 `static Foo()` 即 `.cctor` 符号，`DeclareImplicitStaticConstructor` 的 `GetDeclaredMethod(".cctor")` 守卫自动跳过合成，字段初始化器注入自动并入用户静态构造体

| 阶段 | 特性 | 说明 | 消歧/降级要点 |
|------|------|------|---------------|
| P1 | 顶层 C# 式函数（原 #1） | `public static void Main() {}` / `public int Add(int x)` / Cocoa 式无关键字 `Main(): void` / `Main() {}` | 修饰符后 `标识符 标识符 (` → 类型前置；`标识符 (` + `)` 后 `{`/`:` → Cocoa 式（括号扫描，与 `print("hi")` 全局语句消歧）；归一 FunctionDeclarationSyntax，Binder 零改动 |
| P2 | 声明语法 C# 补齐（小项打包） | **#2 const 局部** `const int x = 10;`；**#6 using 分号** `using Foo;`；**#3 类多接口** `class Foo: IA, IB` | const：`ParseVariableDeclaration` 支持 `const + Type name [= init]`；using：`ParseMembers` 顶层循环跳过孤立 `;`；多接口：`ClassDeclarationSyntax.baseType` → `baseTypes` 列表（对齐接口声明），`BindClassBase` 遍历（首个非接口 = 基类，其余须为接口 → `AddInterface`） |
| P3 | 属性增强 | **#4 访问器修饰符** `public int X { get; private set; }`；**表达式体成员** `public int Area() => _x * _y;` / `public int X => _x;` | 访问器：`PropertyAccessorSyntax` 加 modifiers + `ParsePropertyAccessor` 解析 + `BindPropertyDeclaration` 访问器可见性独立；表达式体：新 `FatArrowToken`，解析器**合成 `{ return expr; }` 块**（方法）或 getter（属性）→ Binder/发射零改动 |
| P4 | 静态构造函数（#5） | `static Foo() { }`（C# 式）/ `static constructor() { }`（Cocoa 式） | Parser 现两条构造路径已接收修饰符；Binder 构造分支：修饰符含 `static` → 生成 `.cctor` 符号（须 0 参、无构造链、禁实例访问）；复用 `.cctor` 发射管道 + 字段初始化注入 |
| P5 | 字符串插值 | `$"Hello {name}"` / `$"{a} + {b} = {a + b}"`（§23） | `InterpolatedStringToken`（字面量片段 + 洞源文本/绝对 Span）→ 逐洞子解析（Span 校正保证诊断定位）→ 降级字符串 `+` 链；`{{`/`}}` 转义；常量折叠不启用 |
| P6 | `foreach` 语句 | `foreach (var x in arr)` / Cocoa 式 `foreach (x in arr)`（§10.7） | 新 `foreach`/`in` 关键字 + ForeachStatementSyntax；`BindForeachStatement` 降级 Bound while 循环（`__i` + `arr.Length` + `arr[__i]`），压 `_loopStack`；**策略点抽象** |
| P7 | `switch` 语句 | §10.8 形式 + C# 惯用形式，多 case 标签 + `when` 守卫 | 新 `switch`/`case`/`default` 关键字；降级嵌套 BoundIfStatement 链（`value == 常量 && when`），节体压 `(breakLabel: switchEnd, null)`，`break`→goto 末尾 label；if/else 天然防 fall-through |

| 边界（本轮不做） | 后续 |
|------------------|------|
| Lambda + 闭包（`=>` 表达式上下文，三后端） | P8 |
| `foreach`(IEnumerable) 枚举器模式 / 泛型集合 | P8+ |
| switch 表达式（`x switch { ... => ... }`） | P8+ |
| 主构造函数 / 可空类型 / 顶层函数 return type 增强 | P8+ |
| 索引器 / 运算符重载 / 事件 / 嵌套类 / 部分类 / `internal`/`protected` | P8+ |

**共用改动**：Lexer 新增 `=>`/`foreach`/`in`/`switch`/`case`/`default`/`$` 插值 + SyntaxFacts 关键字表；Parser 顶层函数分派/表达式体合成/插值洞解析/foreach/switch 节点 + const C# 式/using 分号/多基类型/访问器修饰符；Binder 顶层 static 修饰符确认 + foreach/switch 降级 + 多接口绑定 + 静态构造生成 + 访问器可见性；发射器零改动。测试每阶段 Parser/Binder 单测 + IL e2e 双后端；文档 `语法手册.md` §9/§10/§11/§23 状态 → ✅ + C# 兼容子节。

### 6d. 项目系统（见 `docs/项目格式规范.md`）

- `.coproj` / `.cosln`：**自定义轻量文本格式**（非 JSON）✅ 已实现（6d-1）
- CLI：`coc build -p foo.coproj`、`coc build -p sol.cosln` ✅ 已实现
- 增量构建：源文件哈希缓存，未变更项目跳过编译 ✅ 已实现

**6d-1 已实现、明确留待后续的内容**：`dll`/`cod` 输出格式（解析通过但发射报"not implemented yet"）、`.cod` 库引用（构建报错）、`[imports]` 节（解析通过但构建仅警告）。

### 6e. 类库体系与完整 OOP

- **M1-M4（已落地）**：统一修饰符（public/private/stdcall/cdecl → `Modifiers`）、class 语言（字段/方法/构造/new/this）、.NET dll 输出（`-f library`）、消费 + using（`-r` + Binder 读引用程序集 + namespace/using）——见 `docs/类库设计.md`
- **M5（已落地）**：完整 OOP（继承、`base(...)`/`this(...)` 构造链、virtual/override/abstract/sealed 多态、static、readonly、显式 this.、属性 + IL Property 元数据表）——见 `docs/OOP设计.md`
- **后置**：native class（对象模型 + Rust 所有权）、内置函数→标准库演进、嵌套类/部分类/接口/`internal`/`protected`
- `export fn` 导出语法（native 导出库 dll 输出时生成导出表）

**验收标准**：exe/library 输出可用；Cocoa 类库（含继承/属性）可被 `-r` + `using` 消费（C# 亦可用）；native/.NET 类导入可编译运行；项目系统支持增量构建。

---

## 阶段 7 — 编译器自举重写

**目标**：用 Cocoa 语言（阶段 6 之后的功能）重写编译器本体。

- 组件顺序：Lexer → Syntax → Parser → Binder → Lowerer → IR → 后端
- Native 与 IL 两条路径都要自举
- 阶段 5 的自研 IL 编码器/元数据写入器在 Cocoa 中重写（递归依赖解耦：编译器只依赖阶段 6 语言特性）

**验收标准**：Cocoa 版编译器可编译全部测试用例。

---

## 阶段 8 — 自举验证

```
Stage 0: 现 C# 编译器编译 Cocoa 编译器源码 → B0
Stage 1: B0 编译同一源码 → B1
Stage 2: B1 编译同一源码 → B2
验证: B1 ≡ B2（输出等价）
```

**验收标准**：B1 ≡ B2，且 B2 能编译真实项目。

---

## 阶段 9 —（可选）Native .NET 互操作

- Native 路径通过 CLR Hosting 加载 .NET 程序集并调用
- 依赖 Windows 原生互操作（无第三方）

---

## 历史路线图说明

> 本仓库早期规划（v0.1→v3.10 逐版本对齐 C# 1.0→13 全特性、JSON 格式 .coproj 等）已废弃并删除。新路线图以上述阶段制为准；语言特性范围见 `docs/语法手册.md` 状态标记。
