# Cocoa 编译器使用手册

> 版本：v0.2（Native x86/x64 双后端 + IL 后端）· 最后更新：2026-08-13

---

## 目录

1. [概述](#1-概述)
2. [安装与构建](#2-安装与构建)
3. [编译器命令行 (`coc`)](#3-编译器命令行-coc)
4. [项目文件 (`.coproj`)](#4-项目文件-coproj)
5. [解决方案文件 (`.cosln`)](#5-解决方案文件-cosln)
6. [输出格式](#6-输出格式)
7. [增量构建](#7-增量构建)
8. [交互式环境 (`coi`)](#8-交互式环境-coi)
9. [Cocoa 语言快速入门](#9-cocoa-语言快速入门)
10. [常见问题](#10-常见问题)

---

## 1. 概述

Cocoa 是一个自研编译器，支持两条后端路径：

- **Native 路径**：x86 / x64 机器码 + 自研 PE 输出（零第三方依赖）
- **IL 路径**：ECMA-335 IL（自研编码器 + 元数据写入器，阶段 5 完成）
- **IL 互操作**：native DLL 经 ImplMap/ModuleRef 消费（阶段 5-5 落地）；Native 路径导入表（阶段 6）

支持：

- 直接编译 `.co` 源文件
- 通过 `.coproj` 项目文件组织多源文件项目
- 通过 `.cosln` 解决方案文件组织多项目解决方案
- 三种输出格式：`exe` / `dll` / `cod`
- native DLL 导入（`import`）、`.cod` 库引用（阶段 6）
- 增量构建（阶段 6）

### 工具链组成

| 工具 | 用途 |
|------|------|
| `coc` | 命令行编译器 |
| `coi` | 交互式 REPL |
| `coc.exe` / `coi.exe` | 便捷调用脚本（`src/coc.cmd` / `src/coi.cmd`） |

---

## 2. 安装与构建

### 2.1 前置要求

- .NET SDK（9.0）
- 操作系统：Windows / Linux / macOS

### 2.2 构建编译器

```bash
# Windows
src\build.cmd

# Linux/macOS
src/build.sh
```

### 2.3 验证安装

```bash
coc --help
```

---

## 3. 编译器命令行 (`coc`)

### 3.1 命令结构

```bash
coc build <source> [options]     # 编译（新子命令形态，阶段 6）
coc <source> [options]           # 直接编译（兼容形态）
```

`<source>` 支持：

| 输入 | 行为 |
|------|------|
| `hello.co` | 编译单文件 |
| `a.co b.co c.co` | 编译多文件 |
| `MyApp.coproj` | 编译项目 |
| `MyApp.cosln` | 编译解决方案（拓扑排序） |

### 3.2 选项说明

| 选项 | 说明 | 示例 |
|------|------|------|
| `-p <path>` | 项目/解决方案文件（与位置参数等价） | `coc build -p foo.coproj` |
| `-f <format>` | 输出格式：`exe` / `dll` / `cod`（默认 exe） | `coc build -p foo.coproj -f dll` |
| `--platform <x86\|x64>` | Native 目标平台（默认 x64） | `coc --platform x86 hello.co` |
| `-o <path>` | 输出路径 | `-o output.exe` |
| `-r <path>` | 引用 `.cod` 库（可多次） | `-r lib.cod` |
| `--no-incremental` | 强制全量构建 | `coc build -p foo.coproj --no-incremental` |
| `--debug` / `--release` | 构建模式（默认 release） | `coc build -p foo.coproj --debug` |
| `-?` / `-h` / `--help` | 帮助 | `coc -h` |

### 3.3 示例

```bash
# 单文件 → exe
coc hello.co

# 指定平台与输出
coc --platform x86 hello.co -o hello-x86.exe

# 项目 / 解决方案
coc build -p src/MyApp/MyApp.coproj
coc build -p src/MyApp.cosln

# 输出 dll / cod
coc build -p foo.coproj -f dll
coc build -p foo.coproj -f cod

# 引用 .cod 库
coc main.co -r ../Libs/mylib.cod
```

---

## 4. 项目文件 (`.coproj`)

采用**轻量文本格式**（非 JSON）。完整规范见 [`docs/项目格式规范.md`](项目格式规范.md)。

```
name = MyApp
output = exe
platform = x64
entry = main

[sources]
src/*.co

[references]
lib.cod

[imports]
kernel32.dll

[options]
incremental = true
debug = false
outputPath = bin
```

| 字段 | 说明 |
|------|------|
| `name` | 项目名（默认 = 文件名） |
| `output` | `exe` / `dll` / `cod` |
| `platform` | `x86` / `x64` |
| `entry` | 入口函数（仅 exe） |
| `[sources]` | 源文件 glob 列表 |
| `[references]` | `.cod` 库列表 |
| `[imports]` | native DLL 列表 |
| `[options]` | 增量/调试/输出目录 |

---

## 5. 解决方案文件 (`.cosln`)

```
name = MyApp

[projects]
src/Core/Core.coproj
src/App/App.coproj
```

编译顺序由项目间 `.cod` 引用拓扑排序决定，无依赖时按声明顺序。

---

## 6. 输出格式

| 格式 | 说明 | 详见 |
|------|------|------|
| `exe` | 可执行文件（Native PE 或 IL 程序集） | `docs/输出格式.md` |
| `dll` | 导出库：导出表 + 重定位表，`export fn` 标记导出 | `docs/输出格式.md` |
| `cod` | IR 序列化库，编译期合并 | `docs/项目格式规范.md` §4 |

---

## 7. 增量构建

- 默认开启（`incremental = true`）
- 缓存：`<outputPath>/.coc-build/<name>.cache`（源文件 SHA-256 + 选项快照）
- 全命中 → "up to date"；部分变化 → 仅重编相关项目
- `--no-incremental` 强制全量

---

## 8. 交互式环境 (`coi`)

```bash
coi                    # 启动 REPL
echo "print('hi')" | coi   # 从 stdin 执行
```

| 命令 | 说明 |
|------|------|
| `#help` | 帮助 |
| `#exit` | 退出 |
| `#r "path"` | 引用程序集 / .cod |
| `#load "file.co"` | 加载源文件 |
| `#submit` | 提交并执行 |

---

## 9. Cocoa 语言快速入门

### 9.1 Hello World

```cocoa
// hello.co
function main()
{
    print("What's your name?")
    let name = input()
    print("Hello " + name + "!")
}
```

```bash
coc hello.co
./hello.exe
```

### 9.2 变量与类型

```cocoa
let x = 42              // 不可变
var y = 10              // 可变
let name: string = "Alice"
var count: int = 0
let a: int = 42
let b: double = 3.14
let c: bool = true
```

### 9.3 控制流

```cocoa
if (x > 0) { print("positive") }
else if (x == 0) { print("zero") }
else { print("negative") }

var i = 0
while (i < 10) { print(i); i = i + 1 }

for (var i = 0 to 5) { print(i) }

do { print("once") } while (false)
```

### 9.4 函数

```cocoa
function add(a: int, b: int): int { return a + b }

function factorial(n: int): int
{
    if (n <= 1) { return 1 }
    return n * factorial(n - 1)
}
```

### 9.5 导入 native DLL（阶段 6）

```cocoa
import kernel32.dll

export function Add(a: int, b: int): int { return a + b }
```

---

## 10. 常见问题

### 10.1 编译器报 "file doesn't exist"

检查源文件路径。支持相对路径和绝对路径。

### 10.2 崩溃 / 行为异常（Native x86）

阶段 0 已知问题：x86 管道输入场景可能崩溃，正在修复。可先使用 `--platform x64`。

### 10.3 输出文件

| 格式 | 产物 |
|------|------|
| `exe` | `.exe` |
| `dll` | `.dll` |
| `cod` | `.cod` |
| 调试 | `.pdb`（IL 路径）|

---

> 参考文档：
> [`docs/语法手册.md`](语法手册.md) — 语言语法参考
> [`docs/项目格式规范.md`](项目格式规范.md) — coproj/cosln/cod 格式规范
> [`docs/输出格式.md`](输出格式.md) — 输出格式规范
> [`docs/开发计划.md`](开发计划.md) — 阶段路线图
